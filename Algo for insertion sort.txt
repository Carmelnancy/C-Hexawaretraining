Algo for insertion sort:

begin 
step 1:read array elements
step 2:traverse the elements from second element(ist index) till the last
step 3:store the present element in a variable
step 4:compare that variable element with all its previous(left) element
step 5:if variable element is smaller than the previous compared element ,swap the elements
step 6:repeat this process for all elements
end